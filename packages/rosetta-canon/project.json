{
  "name": "rosetta-canon",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/rosetta-canon/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project rosetta-canon --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "vitest run packages/rosetta-canon/src/lib/rosetta-canon.spec.ts"
      },
      "cache": true,
      "inputs": [
        "default",
        "^production",
        "{workspaceRoot}/vitest.config.ts",
        {
          "externalDependencies": [
            "vitest"
          ]
        }
      ],
      "outputs": [
        "{projectRoot}/coverage"
      ]
    }
  }
}
