# ETR-2026-07 editorial experience

Incomplete foundation for #1603 and chapter issues #1604–#1620. Foundation checkpoint:
`1997b3e` on `codex/1603_ETR-2026-07`. The user rejected this result as ordinary articles;
the chapter-specific immersive visual and interactive experience remains unfinished.
The checks below were premature and do not establish issue completion.

## Content and ownership

Sixteen chapter pages and a reference appendix live beneath
`/articles/accelerating-the-dystopia/`. Explicit `series.id` and `series.order`
metadata select these routes; ordinary publications keep their existing renderer.
Navigation is derived from validated consecutive chapter positions. Every page is
discoverable through the Articles index and existing taxonomy/archive surfaces.

Only the exact section, addition, and registry URLs named in the issues were read.
All twelve retained additions were already included verbatim in their corresponding
sections. They appear once. No consolidated manuscript, package crawl, additional
evidence files, or packaged graphics were used.

`content/editorial/accelerating-the-dystopia/migration.json` records source URLs and
SHA-256 hashes. All sixteen source bodies match the migrated prose after whitespace
and equivalent Markdown emphasis normalization. All 95 CSV rows, including every
registry field, are preserved in `sources.json`. That single dataset supplies both
chapter popovers and reference entries. Non-web source locations remain plain text;
they are not converted into invented public links.

## Editorial system

`EditorialSeriesLayout.astro` and `EditorialChapterNav.astro` own the shared shell.
The series keeps the global header/footer and inherited system typeface. Larger
chapter headings, section-opening passages, chapter numerals, alternating quiet
surfaces, and a bounded prose measure establish editorial rhythm. These scoped
typographic choices apply only to this experience.

All CSS lives in `src/styles/editorial/`; the global stylesheet is unchanged.
The authored chapter sections provide narrative beats without duplicated quotations
or rewritten prose. No speculative charts, implied causal measurements, new media,
web fonts, or image-license dependencies were introduced. The only decorative motion
is a small CSS scroll-linked chapter-number translation, enabled only when supported
and reduced motion is not requested. Reading order and information never depend on it.

Native source links are the no-JavaScript baseline. Small progressive enhancement
adds viewport-bounded native popovers, keyboard/pointer/touch access, Escape and
outside dismissal, and dynamic sticky-navigation clearance. Browser find, source
fragments, text selection, history, and print retain their ordinary behavior.

## Validation — September 17, 2026

Only six new tests were authored: four unit cases for consecutive series ordering
and source-key resolution, plus two browser cases for novel citation interaction.
They use synthetic source identities or `data-test-id` hooks and dynamic destinations;
none asserts manuscript wording, CSS classes/IDs, or DOM hierarchy.

- Nx `build`, `typecheck`, `lint`, and `format:check`: pass.
- Site unit suite: 21 pass. New logic followed red/green development.
- Full Playwright suite: 214 pass, including generated-page axe audits.
- HTML, duplicate-ID, local-link, asset, and fragment validation: pass across 194 pages.
- All 17 pages at 320, 390, 768, 1024, 1366, and 1536 CSS pixels: no document overflow.
- All 17 pages at 320px with 200% text size and WCAG text-spacing overrides: no overflow.
- Reduced-motion mode: no active animations; content remains visible.
- JavaScript disabled: all 17 routes return 200 and retain 17 real chapter links;
  sampled source navigation lands below the sticky navigation.
- Print: navigation is hidden and source links remain visible.
- Opening and reference pages: Lighthouse mobile/desktop scores are 100 for
  performance, accessibility, best practices, and SEO. Measurements were taken after
  implementation; the subsequent two-digit enlarged-text fix does not change those
  normal-size layouts. Existing homepage mobile performance measured 92; it is outside
  this change. The existing cost report measured 99; other baseline scores were 100.
- Series-specific client module: 1,870 uncompressed bytes / 818 bytes gzip, below the
  8 KiB gzip budget. No added media. Existing global analytics remains unchanged.

The existing HTML validator initially exhausted its 4 GiB heap while repeatedly
parsing linked documents. It now caches only fragment-ID sets and closes JSDOM windows;
the same whole-site gate completes successfully without raising the heap limit.

Automated axe and HTML checks are bounded evidence, not complete WCAG certification.
Screen-reader user testing and an external W3C Nu service audit were not performed.
Local screenshots, detailed test diagnostics, and Lighthouse JSON remain in the
execution environment; the results above provide the durable review record.

The repository currently lacks the feature-workflow lease/finalization tooling
referenced by the installed skills. No lease, digest cursor, archive, merge, or issue
closure is claimed. The branch is not ready for acceptance review. Finish the editorial
experience before further broad browser, W3C/WCAG, or performance validation.
