# 2026-04-24 — Daily Memory

## Pipeline Chaos + Recovery

### What happened
Docs-intelligence pipeline ran without proper git sync discipline. Sub-agents spawned on stale branches, causing duplicate extractions, multiple PRs for the same doc, and PR #37 merged autonomously (boundary violation). Crates manually reset the workspace and fixed damage.

### DI issues filed
- DI-007: context isolation (agent:main loses context on compact) → GitHub #31
- DI-008: ledger locking (race conditions on doc claims) → GitHub #32
- DI-009: knowledge graph updates (KG not updated per cycle) → GitHub #33
- DI-010: check prior work before creating issues → GitHub #34
- DI-011: sub-agent no-merge boundary → GitHub #39 (PR #37 merged autonomously)
- DI-012: issue draft files mandatory → GitHub #45

### DI-008 fix (PR #76)
Added `git fetch origin main && git reset --hard origin/main` at cycle start + verify clean branch before proceeding. Must be reviewed and merged.

### Workspace git incident
I ran `git reset --hard origin/main` in the wrong repo (`~/.openclaw/workspace/` instead of `Code/rosetta/`). Files were actually intact (staged, not lost), but caused confusion. Workspace files (SOUL.md, IDENTITY.md, USER.md, AGENTS.md, HEARTBEAT.md) all verified intact — Crates had already restored them via `chore(unfscking)` commit (9b59379) and `docs(ledger)` commit (11ad2e6). Pushed `local-main` to origin as a proper branch. BOOTSTRAP.md correctly absent (intentionally deleted per AGENTS.md).

### Current state (end of session)
- Workspace clean, all identity files intact, `local-main` pushed to origin
- Pipeline: ~26 of 128 docs processed (various batches, some duplication)
- PRs: #27, #28, #29, #30, #48, #50, #51, #60, #64, #67, #73, #76 (open/in-progress)
- Sub-agent spawn confirmed working
- Ledger: `/Users/cr8s/.openclaw/workspace/rosetta-di-ledger.md` (outside repo, survives branches)
- Next priority: get PR #76 (DI-008 fix) reviewed and merged; continue extraction loop

## Concepts & Decisions

### Key decisions
- Sub-agents must never merge their own PRs — hard boundary
- Issue draft files mandatory for every issue candidate (zero files when candidates exist = violation)
- Tilde paths for portability: `~/.openclaw/workspace/...`
- Draft column in extraction issue candidates table (file path or GitHub issue link)
- One branch per doc, separate PR — no batching
- Always sync to origin/main before each cycle (DI-008 fix)

### Critical paths
- Repo: `~/.openclaw/workspace/Code/rosetta`
- Ledger: `~/.openclaw/workspace/rosetta-di-ledger.md`
- HEARTBEAT.md: `~/.openclaw/workspace/HEARTBEAT.md`
- Telegram: numeric chat ID `8740875131` (ClawbAppleBot)
- Sub-agent never-merge hard rule: `gh pr merge`, `gh pr close`, `gh pr review --approve`, any PR state change — FORBIDDEN

## Critical insight: personal creative chats are gold mines
MOST ideation comes from personal creative transcripts. The "personal-chat" tag is NOT a filter signal — it means the file originated from a chat session, not that it's off-topic. Files like Spatial Data Fabric, Semantic Audio Cognition Framework, Treating Trends Tantamount to Trading Technicals — these are foundational creative work showing Entif/Rosetta concept evolution with full technical reasoning. Never pre-filter on "personal-chat" label alone. Process first, assess value from findings.

## Snags / Blockers
- Sub-agents ran concurrently without locking → duplicate extractions (OMOC, Pasigraphy, etc.)
- Workspace git was accidentally operated on (wrong repo) — resolved, all files intact
- Ledger still has some stale lock entries on already-processed docs
- Batches 4–5 (~106 docs) remain to process