# RRP Tests

The current runtime tests cover:

- signed receipt verification
- incomplete bundle rejection
- deterministic tile digests

Future slices should align these runtime tests with the pack test vectors more directly.
