# Source Substrate Tests

The current package-level tests assert:

- source system, record, manifestation, and trust matrix stay distinct
- registry entries round-trip as versioned artifacts
- canonical cache lifecycle changes do not erase prior data
